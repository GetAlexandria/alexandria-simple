# Alexandria Marketplace

This package owns the shared Claude plugin marketplace metadata for Alexandria.

Deployment tooling uses this marketplace root when assembling the public
`GetAlexandria/alexandria` repository. Alexandria now ships one plugin payload:

- `packages/alexandria-plugin`
