target: frame
check failed: Verbatim quotes
exact text that failed it:

```text
Half my best source material dies in tabs.
```

reason: Not findable character-exact in the transcript; the transcript has the line break between `source` and `material`.
strike count: 3

---

target: relate
check failed: Verbatim quotes
exact text that failed it:

```text
"I can't remember which tab had the thing"
```

reason: Not findable character-exact in the transcript; the transcript has the line break between `remember` and `which`.
strike count: 1

---

target: relate
check failed: Verbatim quotes
exact text that failed it:

```text
"they don't know what's already in the library"
```

reason: Not findable character-exact in the transcript; the transcript has the line break between `in` and `the`.
strike count: 1
