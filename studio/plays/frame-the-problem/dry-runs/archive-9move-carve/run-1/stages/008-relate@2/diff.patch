diff --git a/runtime/related-brief.md b/runtime/related-brief.md
index 1012af7..4f479c4 100644
--- a/runtime/related-brief.md
+++ b/runtime/related-brief.md
@@ -148,7 +148,7 @@ This names a possible implementation shape, not a problem frame.
 
 ## Relationships
 
-- `disputed` — `I find source material while researching, then lose it before conversion` ↔ `I don't know what's already in the library, so I re-add things`: Maya vs DEV over whether the entries belong together as "all the same thing" in "the extension" or split into capture and search. Candidate edges in the dispute: Maya = `unclear` via the extension bundle; DEV = `sibling` via "That's a different thing though. That's search." Test: review observed research and conversion incidents; tag each by the quoted failure mode, "I can't remember which tab had the thing" / "So stuff just never makes it in" or "they don't know what's already in the library" / "so they re-add things," then check whether the same incidents contain both failure modes or separate by circumstance.
+- `disputed` — `I find source material while researching, then lose it before conversion` ↔ `I don't know what's already in the library, so I re-add things`: Maya vs DEV over whether the entries belong together as "all the same thing" in "the extension" or split into capture and search. Candidate edges in the dispute: Maya = `unclear` via the extension bundle; DEV = `sibling` via "That's a different thing though. That's search." Test: review observed research and conversion incidents; tag incidents by the quoted failure mode, "I can't remember which tab had the thing" / "So stuff just never makes it in" or "they don't know what's already in the library" / "so they re-add things," then check whether the failures appear in shared incidents or split by circumstance.
 
 ## Hunch
 
