I’ll read the brief, transcript, available context files, and existing strike ledger, then apply the ground checks without rewriting the content.The read commands hit the environment’s sandbox launcher issue, so I’m re-running the same file reads outside the sandbox with approval semantics. I’ll keep this to inspection commands first.I’ve got all supplied files. The existing strike ledger already tracks the transcript line-break mismatch for “Half my best source material dies in tabs.”; now I’m checking whether anything else mechanically fails before deciding whether to bounce or annotate.The header matches the supplied context set, and the hunch is explicitly `none earned`. The remaining question is whether the quote checks and lexicon scan require another frame bounce.The fenced transcript quotes are mostly exact substrings, but the existing `Half my best source material dies in tabs.` mismatch is still present. I’m doing one last exact scan for the banned sizing/sequencing words outside fenced quotes before writing the ledger.Only the carried quote mismatch is failing under the closed checks, and it still has strikes remaining. I’m rewriting `runtime/bounce-note.md` with that item’s strike count advanced and routing it back to frame.The ledger edit needs to be exact rather than additive, so I’m correcting the strike line now.The bounce ledger now contains the single unresolved frame item with strike count 2. I’ll verify that and then close with the required routing decision.Wrote `runtime/bounce-note.md`.

Finding: verbatim quote still fails because the brief has:

```text
Half my best source material dies in tabs.
```

Transcript has the line break between `source` and `material`:

```text
which tab had the thing. So stuff just never makes it in. Half my best source
material dies in tabs.
```

Strike count is now 2.

{"preferred_next_label": "Fix entries"}