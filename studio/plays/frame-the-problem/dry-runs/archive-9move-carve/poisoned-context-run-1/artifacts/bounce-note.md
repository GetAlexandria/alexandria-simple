# Bounce Note

target: frame

---

## Item 1 — Check 1 (verbatim quotes), strike 1 of 3

P1 Evidence 7 uses single quotes where the transcript has double quotes.

Brief text:
`"You spend the mental energy on 'is this deliberate?' instead of 'is this actually okay for us?'"`

Transcript text (line 15):
`You spend the mental energy on "is this deliberate?" instead of "is this actually okay for us?"`

The inner quote marks must be `"` (double), not `'` (single), to be character-exact.

---

## Item 2 — Check 4 (required fields), strike 1 of 3

Problem 2 has no **Insight** field. Problem 1 has an explicit **Insight** section; Problem 2 does not. Every entry requires insight per the field rules.

---

## Item 3 — Check 5 (sizing lexicon), strike 1 of 3

P2 Who field contains the word "quickly" (triggers the "quick" filter).

Brief text (P2 Who):
`"needs to quickly orient to where things stand without reading every version from scratch"`

The word "quickly" must be replaced with a non-sizing synonym.

---

## Item 4 — Check 1 (verbatim quotes), strike 1 of 3 — target: relate (held for next bounce)

Relationships section quote is missing the trailing period:
`"I think it's version tracking — like, where did each open issue end up"`

Transcript (line 21) has:
`I think it's version tracking — like, where did each open issue end up.`

The period before the closing quotation mark is missing. This item targets `relate` and will be bounced after frame items are resolved.
