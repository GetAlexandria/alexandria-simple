# Archive — 9-move carve runs (SUPERSEDED, do not read as Riff results)

> **⚠️ These runs measured the retired 9-move pipeline, not the Riff play.**

Every run record in this folder was produced by the pre-Riff 9-move carve
(`locate → extract → frame → relate → ground → render → word_check → self_check
→ cold_reader`). They emit the old artifacts — `problem-brief.md`,
`evidence-list.md`, `cold-read-report.md`, `self-check-verdict.md` — and **none**
emit the Riff outputs `runtime/problem-framing.md` / `runtime/for-the-director.md`.

The 2026-06-17 Riff re-architecture replaced that graph with
`pre_fill → review ⇄ revise → exit`. These records were moved here on 2026-06-18
so the live play's `dry-runs/` and `risk-map.md` stop presenting them as the Riff
play's results (which they are not). They are kept as a curriculum record of the
carve's behavior and grading.

**No graded Riff run exists yet.** The fresh grading campaign runs on the Riff
play, against the re-tuned fixtures; its records land in `../` (alongside this
archive), and `../../risk-map.md` carries the `results: owed` state until then.
