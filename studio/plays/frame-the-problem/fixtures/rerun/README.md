# Case: rerun — the revise loop folds in new evidence

The conversation has moved on since an earlier framing. New evidence has landed
(P2's director claim is finally anchored to a named, dated instance; a genuinely
new coverage-bar problem is voiced), so the framing should change in response —
without discarding what still holds.

> **Note — Riff mapping.** The old 9-move play fed a separate `prior_brief`
> input to signal "revise, don't start fresh." The Riff play has no such input:
> revision is the `review ⇄ revise` loop, where the director reacts to the
> current `runtime/problem-framing.md` and `revise` folds his reaction in. This
> transcript carries the moved-on conversation; the "what still holds" baseline
> is the current draft in the workspace, not a supplied prior file. This case's
> behavior-under-test (carry-forward, evidence upgrade, resist priority bait)
> maps to a single `revise` pass — but the original "first run vs. rerun"
> framing no longer maps cleanly and the case may want a redesign that exercises
> the loop explicitly.

## Inputs provided

| Input | File | Source (frozen original) |
|---|---|---|
| `transcript` | `transcript.md` | `fixtures/meeting-snippet-01-continued.md` (content after the preamble `---`) |

Single workflow input: `transcript` (the handed-in material — the moved-on
conversation).

## Planted properties (the answer this case tests for)

Moved here from the frozen fixture's documentation preamble:

1. **P2's evidence upgrades.** The previously-secondhand director re-add claim
   is now backed by a concrete past instance with a named director (Priya
   banked the same board deck twice, caught Tuesday).
2. **A genuinely new problem is voiced.** Duplicates corrupt the coverage bars
   and nearly drove a bad call (a near-skipped conversion session).
3. **Maya concedes the one-thing-or-two dispute on the record** ("Okay, fine —
   it's two things.").
4. **Fresh priority bait** — "But mine's still the bigger one. Can we do mine
   first?" The play must not start sequencing/prioritizing in response.

## Expected correct outcome

A revised `runtime/problem-framing.md` that carries forward what still holds,
upgrades P2's evidence grade now that a named specific-past instance exists,
adds the newly-voiced coverage-bar problem with its evidence, records Maya's
concession, and resists the priority bait (no sizing/sequencing language in
Raven's own words).
