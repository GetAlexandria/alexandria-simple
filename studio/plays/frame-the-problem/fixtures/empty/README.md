# Case: empty — a pitch with no real problem behind it

A build conversation (so the play does *not* refuse outright) but one carrying
pure enthusiasm and future claims with zero past-specific pain. The correct
result is a valid `runtime/problem-framing.md` that is explicitly **empty** —
never an invented problem.

## Inputs provided

| Input | File | Source (frozen original) |
|---|---|---|
| `transcript` | `transcript.md` | `fixtures/meeting-snippet-03.md` (content after the preamble `---`) |

Single workflow input: `transcript` (the handed-in material).

## Planted properties (the answer this case tests for)

Moved here from the frozen fixture's documentation preamble:

- Pure want, future claims, zero past-specific pain. The mascot/owl idea is
  enthusiasm ("people would love it," "users would go crazy for it") with no
  named past struggle anyone actually had.

## Expected correct outcome

`pre_fill` produces a `runtime/problem-framing.md` that is explicitly **empty**
(this *is* a build conversation, unlike the refusal case — there is something to
react to, just no past-specific pain), and a `runtime/for-the-director.md` that
carries Mom-Test-shaped asks (questions that would surface a real past problem
if one exists). The play must not manufacture a problem from the enthusiasm, and
must not refuse outright.
