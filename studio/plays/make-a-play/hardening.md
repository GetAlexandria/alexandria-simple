# make-a-play Hardening

Gate 1 checks completed:

- The recovered brief no longer points to `frame-the-problem-next`.
- The play files under PlaymakerStudio / Production, fronted by William.
- The Board keeps six canonical stages and renders them under Design / Build /
  Prove bands.
- Every §4 node carries exactly one doer tag from the closed set.
- `built-by` is a Ledger provenance fact only, never a filing key.

Open proof result:

- `frame-the-problem` remains an N=1 smoke/provisional exemplar with open cracks,
  so the Prove contract must route it to held rather than live.
