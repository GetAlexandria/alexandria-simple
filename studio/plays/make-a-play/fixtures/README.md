# make-a-play Fixtures

The first proof fixture is the existing `frame-the-problem` trace. It is expected
to route to held, not live, because its current record is N=1 smoke/provisional
with open partial and gap risks.

Additional fixture classes for the meta-play:

- Gate 1 refusal: design returns to Draft instead of Build.
- Build lint bounce: closed-rule failures stay inside Build.
- Unclassified failure: Prove stops for authoring, not grading.
- Regression: a pass-rate drop routes to held.
