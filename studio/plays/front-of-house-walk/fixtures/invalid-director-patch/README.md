# invalid-director-patch fixture

EL3 negative fixture for provenance validation. The bundle carries a patch that
claims a director-backed card update but references a nonexistent user answer
event. AX must reject the patch and leave cards unchanged.
