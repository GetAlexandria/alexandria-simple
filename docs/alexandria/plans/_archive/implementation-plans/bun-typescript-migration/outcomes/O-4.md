---
id: O-4
title: Shared TS graph library replaces lib/graph.py with equivalent functionality
tier: must
cards: [System - DAG Engine, System - Quality Grading Engine, Capability - Linting]
---

## Validation Criteria

- `src/lib/graph.ts` provides card parsing, wikilink extraction, graph traversal
- All 43 existing graph library tests pass against the TypeScript implementation
- `src/lib/frontmatter.ts` handles all frontmatter fields including `requires:` capability schema
- Shared CLI helpers used by all tools
- `lib/graph.py` can be deleted with no remaining Python imports

## Motivation

`lib/graph.py` is a 535-line shared Python library used by grade, lint, retrieve,
tensions, and wizard tools. Porting it to TypeScript is the critical enabler for
all tool rewrites. The 43 existing tests document exact behavior — porting
test-by-test ensures no regressions.
