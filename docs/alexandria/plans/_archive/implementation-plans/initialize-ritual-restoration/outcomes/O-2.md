---
id: O-2
title: /library is a pure router; first-session and returning-session are separate jobs
tier: must
cards: [Agent - Raven the Maven, Artifact - Decision: Single Entry Point]
---

## Validation Criteria

- `raven.md` Job Dispatch table lists `first-session` and `returning-session` as distinct jobs with separate procedure files
- `skills/raven/job-initialize.md` is deleted
- `skills/raven/job-first-session.md` and `skills/raven/job-returning-session.md` exist and are each focused on one scope
- Router logic in raven.md is declarative (table + trigger conditions) with zero procedural flow
- Single Entry Point decision preserved: the human still only ever invokes `/library`

## Motivation

The procedure-shape problem is that a ritual (first-session) was expressed as a branch inside a decision-tree procedure (room management). Separating the jobs restores the ritual's integrity and gives returning-sessions the flexibility they genuinely need. The router pattern makes the split invisible to the human — Single Entry Point is preserved.
