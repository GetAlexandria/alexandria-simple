---
id: O-1
title: First-session ritual restored end-to-end on fresh /library
tier: must
cards: [Agent - Raven the Maven, System - Wizard Configuration Engine, Artifact - Decision: Single Entry Point]
---

## Validation Criteria

- `/library` on an empty project (no `alexandria-config.json`) runs the ritual in this order: opening (team intro + consent to scan) → codebase scan → noun-proposal dialogue → configuration with explicit confirmation gate → engine run producing tier assignments → gap analysis beat → starter cards written → scoreboard displayed → Conan grading handoff queued
- Eval case for empty-project first-session hits every beat in order; structural check asserts beat ordering, LLM-as-judge scores ritual coherence ≥ old-wizard baseline
- No beat is skipped under Opus load — verified across three eval runs

## Motivation

The FEAT-045 wizard→library collapse inadvertently demoted the first-session ritual from a linear 7-step play to a branch inside a room-management procedure. The ritual lost its un-skippable character and users report a disconnected, drunk-project-manager experience. This outcome restores the ritual. It's the core user-facing deliverable.
