---
id: O-6
title: Cleanup — kill assessment.md, strike session_notes, fix noun-dialogue orphan
tier: could
cards: [Agent - Raven the Maven]
---

## Validation Criteria

- `assessment.md` is no longer written as a persisted artifact by first-session; `assessment-generation.md` and assessment template in `output-formats.md` are pruned or deleted
- `session_notes` field removed from `alexandria-config.json` schema, `job-first-session.md` / `job-returning-session.md`, and any remaining references
- No dangling references to `assessment.md`, `session_notes`, or orphaned `noun-dialogue.md` invocations remain in skills, agents, templates, or docs (noun-dialogue wiring itself is FEAT-062's responsibility under O-1; this outcome verifies nothing stale survives elsewhere)

## Motivation

Surface-area reduction. Each of these is cheap on its own and removes a source of drift between documented behavior and actual behavior. Deferring them means re-opening the same files in a follow-up plan. Tier: Could because none block the ritual restoration, but they're opportunistic value while the files are already open.
