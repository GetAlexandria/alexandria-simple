---
id: O-3
title: Lint targets have human-readable names used consistently across skills and docs
tier: should
cards: [Agent - Nit the Picker]
---

## Validation Criteria

- All 11 lint targets have descriptive names (lines, cards, graph, layers, library, paths, plans, wizard, counts, grades, briefings)
- `skills/nit/sweeps.md` uses names instead of (or alongside) numbers
- Nit agent definition, Conan health-check skill, and any library cards referencing "sweep 1" etc. are updated to use names
- CLI accepts names only (no numeric aliases needed)

## Motivation

"Sweep 3" means nothing without looking it up. "graph" is immediately clear. Named targets make the CLI self-documenting and align the skill definitions with what users actually type.
