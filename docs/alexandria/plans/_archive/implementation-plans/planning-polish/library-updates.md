# Library Updates from Planning Polish

Ask Conan to review this list and produce a transient surgery plan for Sam in the conversation, not as a checked-in file.

## Card updates

| Action | Card | What Changed | Source |
|--------|------|-------------|--------|
| Create | Artifact - Decision: Skill Naming Convention | Plugin auto-namespace means skills use short `name:` fields. Claude Code prefixes as `/alexandria:<name>`. No manual prefixing needed. Decided during planning-polish planning session. | Planning Decision 1 |
| Update | Agent - Raven the Maven | Flag as needing wizard-mode capability update (Danvers's scope -- Raven will drive the wizard experience and skill may rename from `wizard` to `library`). Note only; no card content change in this plan. | Planning context |
