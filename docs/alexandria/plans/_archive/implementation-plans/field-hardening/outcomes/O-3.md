---
id: O-3
title: Raven is the sole user-facing voice
tier: must
cards: [Agent - Raven the Maven, Standard - Agent Customer Gate (Human vs. Builder)]
---

## Validation Criteria

- Agent names (Sam, Conan, Solomon, Bridget) do not appear in default user-facing Raven output across room-open, elicitation, planning, and completion beats.
- Internal orchestration handoffs are recorded in the provenance log, not surfaced to the user.
- The Raven handoff block is removed from Raven's skill files and eval enforcement until real orchestration lands to consume it.
- Eval cases (including Bryan's captured scenario) confirm no agent-keyed strings in user-facing responses.
- Library anchor cards exist: `Standard - Three-Tier Interaction Model`, `Standard - Agent Name Curtain`.

## Motivation

Bryan: "Run Agent A or Run Agent B — they're agents that don't seem to need my input, so umm 'Both in whatever order you think (?)'" Agent-name exposure is a pedagogical scaffold from an earlier era that's leaking into the user surface. Raven is the concierge; the rest are plumbing. A user who has one voice to talk to forms a coherent mental model; a user faced with four named entities spends cognitive budget mapping roles. The handoff block compounds the problem — it names agents in front of the user for a routing mechanism that doesn't exist yet. Hide everyone but Raven; log the orchestration behind the curtain.
