# Library Updates from Phase 2: Raven Orchestrates the Wizard

Ask Conan to review this list and produce a transient surgery plan for Sam in the conversation, not as a checked-in file.

## Card updates

| Action | Card | What Changed | Source |
|--------|------|-------------|--------|
| Create | Artifact - Decision: /library as Persistent Room | Entry point is `/library`, not `/wizard`, `/assemble`, or `/initialize`. The command names what you're building, not the action. `/library` is a persistent room — Raven picks up where she left off across sessions. The iterative model: build enough to ship, ship, come back, fill more, unlock more. Not a one-time command. | Planning Decision 1 |
| Create | Artifact - Decision: Scoreboard Derivation Algorithm | Scoreboard fill levels are derived from alexandria-config.json + current library state on every session start. Not persisted separately. Five states: Nothing (0%) / Raw material dumped (25%) / Elicitation done (50%) / Cards drafted, questions remain (75%) / Functioning (100%). Derived at runtime means always current — any agent can update state, Raven reads truth. | Planning Decision 3, LIB2-002 |
| Create | Artifact - Decision: Raven Elicits, Writers Stay Specialized | In wizard-mode, Raven writes confirmed config artifacts directly (`alexandria-config.json`, `assessment.md`) and directs Sam only for card-like artifacts. This removes unnecessary config-artifact handoffs while keeping card authorship, structure discipline, and later quality review in the Sam path. The "Raven never directs Sam" rule applies only to the product conversation job — wizard-mode remains the explicit exception for card-like artifact work. | Planning Decision 2, Issue 381 |
| Update | Agent - Raven (WHEN) | Wizard-mode is now a second job in Raven's job dispatch table. When invoked via `/library`, Raven enters wizard-mode: reads library state, renders scoreboard, conducts session, delegates artifact production to Sam. Wizard-mode is distinct from product conversation — different goal, tools, stopping criteria, and relationship to Sam. | LIB2-004 |
| Update | Capability - Library Configuration (HOW, WHEN) | Library configuration now occurs through Raven's wizard-mode conversation rather than a linear wizard skill procedure. Configuration questions (AI mode, novelty, complexity) are answered through dialogue and inference, not a form. The wizard engine YAML and output artifacts are unchanged; only the experience layer changes. | LIB2-001, LIB2-004 |
| Create | Artifact - Expert Calibration Reference | Practitioner heuristics for library construction, encoded as a reference skill Raven loads in wizard-mode. Covers: Frankenstein diagnostic, scoreboard shape mismatch detection, guidance gap pattern, hypothesis problem, guidance posture spectrum (prescriptive/advisory/transparent), unlock logic, fill states, PULL as background lens, first-five-minutes sequence, cross-cutting principles. Source: expert interview 2026-04-02. | LIB2-005 |
| Update | System - Wizard Engine (WHEN) | Scoreboard derivation algorithm is now a specified component of the wizard engine system. Fill levels computed from library state per derivation spec at `docs/initialize/scoreboard-derivation.md`. ASCII renderer produces bucket display (Foundation/Core/Amplifier) with bar-fill characters. Clearance and lock messages derived from bucket aggregates. | LIB2-002, LIB2-003 |
